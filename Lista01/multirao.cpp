#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

struct node {
    // build the function and methods
};

struct queue {
    // build the function and methods
};

struct stack {
    // build the function and methods
};

int main(int argc, char *argv[]) {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int e, process, id, p_hours, w_hours;
    cin >> e;

    for (int i = 0; i < e; i++) {
        
    }
    

    return 0;
}